# Public warehouse layout data

The files in this directory are intentionally public reference assets used to
describe the warehouse floor plan. They contain geometry and stable public
labels only. They must not be replaced with database backups or exports.

`warehouse-zones.csv` omits database row IDs and timestamps. Inventory records,
user records, audit data, uploaded documents, and operational exports do not
belong here.